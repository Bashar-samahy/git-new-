name: Simple Workflow

on:
  push:
    branches:
      - main
    paths: 
      - "src/**"
  pull_request:
    branches:
      - main 
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: run tests
        run: npm test
  